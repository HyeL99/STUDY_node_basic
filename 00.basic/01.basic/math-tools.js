const PI = 3.14;
const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;

// exports.add = add;
// exports.plus = add;

module.exports = {PI, add, subtract, multiply, divide};